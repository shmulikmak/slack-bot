{
  "name": "slack-bot",
  "version": "1.0.0",
  "description": "slack bot - microservice",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shmulikmak",
  "license": "ISC",
  "dependencies": {
    "@slack/client": "^3.14.2",
    "express": "^4.16.2"
  }
}
